[project]
name = "comfyui-prompt-craft"
description = "Stop wrestling with megaprompts. Stack modular blocks (skin, light, outfit…) into clean str_pos/str_neg, pin fields to the sidebar, and reuse prompts from a built-in SQLite library — layouts, pairs, and a full manager included."
version = "1.0.0"
license = { file = "LICENSE" }
requires-python = ">=3.8"
dependencies = []

[project.urls]
Repository = "https://github.com/StoneZol/comfyui-prompt-craft"
Homepage = "https://github.com/StoneZol/comfyui-prompt-craft"

[tool.comfy]
PublisherId = "stone-zol"
DisplayName = "Prompt Craft"
Icon = ""
